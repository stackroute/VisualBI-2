(function(){
   var config = {
      userDashboard: "dashboards",
      container: "#dashboards"
   };
   widgetHandler(config);

   // $('#btnLogout').on('click', function(e) {
   //    e.preventDefault();
   //    $.ajax('logout')
   // });
})();
